"""HealthFit AI Chatbot — Controllers Package

Contains business logic controllers that orchestrate the
flow between HTTP routes, the NLP service layer, and the
database layer. Controllers are HTTP-agnostic — they
receive and return plain Python objects, not Flask
request/response objects.
"""
